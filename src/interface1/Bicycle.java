package interface1;

interface Bicycle {
	void applyBreak(int decrement);   //interfaces contains abstract methods i.e. non concrete methods
	                                  //(methods doesn't contains body or method definition)
	void speedUp(int increment);
}
